{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn 'backend.app:create_app()' --bind 0.0.0.0:8000 --workers=1 --timeout=300 --worker-class=sync --preload --access-logfile=- --error-logfile=- --log-level=info",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
